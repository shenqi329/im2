package bean

import (
//"testing"
)

// func TestEncoderLengthToByte(t *testing.T) {

// 	for messageType, _ := range kinds {
// 		protoMessage := Factory(messageType)
// 		retType := MessageTypeFromMessage(protoMessage)
// 		if retType != messageType {
// 			t.Error("messageType = ", messageType, " retType = ", retType)
// 		}
// 	}
// }
